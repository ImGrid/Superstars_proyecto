API_URL=http://localhost:3001/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# URL canonica del sitio (sin barra final). En produccion: https://superimpact360.lat
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# JWT (mismo secret que el backend, solo server-side — NO usar NEXT_PUBLIC_)
JWT_SECRET=change-me-same-as-backend-jwt-secret
